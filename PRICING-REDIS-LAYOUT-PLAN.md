# طرح یکپارچه‌سازی ذخیره‌سازی در Redis موتور قیمت‌گذاری

## ۱. خواسته

هر تأمین‌کننده باید **فهرست نمادهایی که ارائه می‌دهد** را در بخش Item ذخیره کند،
و در بخش Price **قیمت لحظه‌ای و تاریخچه** را. و صفحه‌ی مقایسه‌ی تأمین‌کنندگان در
پنل درست نمایش دهد.

## ۲. وضعیت امروز — چرا یکپارچه نیست

### الف) فهرست آیتم‌ها (`item:metadata:<provider>:<itemId>`)

هر آیتم **یک کلید جدا** است. نتیجه:

1. **فهرست، فهرست نیست.** هیچ ایندکسی وجود ندارد؛ برای خواندنِ آیتم‌های یک
   تأمین‌کننده `KEYS item:metadata:<p>:*` زده می‌شود که روی کل keyspace اسکن
   می‌کند و Redis را بلاک می‌کند — و این کار در هر بار باز کردن صفحه‌ی اسنپ‌شات
   پنل انجام می‌شود.
2. **TTL روی تک‌تک آیتم‌ها (۲۴ ساعت).** آیتم‌ها جدا جدا منقضی می‌شوند، پس فهرست
   به‌مرور آب می‌رود. اگر متادیتای یک تأمین‌کننده یک روز تازه نشود، نمادهایش
   یکی‌یکی ناپدید می‌شوند — نه همه با هم، که حداقل قابل تشخیص باشد.
3. **آیتمی که تأمین‌کننده دیگر ارائه نمی‌دهد پاک نمی‌شود** و تا انقضای TTL در
   فهرست می‌ماند.
4. `bulkSetMetadata` به‌جای pipeline، N رفت‌وبرگشت جداگانه می‌زند.

### ب) قیمت‌ها

پنج مشکل، که مهم‌ترین‌شان همان «یکپارچه نبودن» است:

5. **سه نمایش هم‌پوشان از یک چیز:** رشته‌ی `price:current:<p>:<id>`،
   SET با نام `price:current:providers:<p>` (که *نام کلیدها* را نگه می‌دارد)، و
   هش `price:snapshot:<p>`. و این سه **هماهنگ نوشته نمی‌شوند**:
   `saveItems` هر سه را می‌نویسد، ولی `setCurrentPrice` فقط دوتای اول را. یعنی
   `price:snapshot:<p>` از قیمت‌های واقعی عقب می‌افتد.
6. **SET هرگز پاک نمی‌شود.** اعضایش نام کلیدهایی‌اند که بعد از یک ساعت منقضی
   می‌شوند، ولی خود SET نه TTL دارد و نه هرس می‌شود. پس تا ابد پر از عضو مرده
   می‌ماند. `getAllCurrentPrices` بعداً برای هر عضو یک GET می‌زند و نبودها را
   بی‌صدا دور می‌اندازد.
7. **همین SET است که «تأمین‌کننده‌های فعال» را تعریف می‌کند.** بک‌اند در
   `getProviders()` با `KEYS price:current:providers:*` فهرست می‌سازد — پس
   تأمین‌کننده‌ای که هفته‌هاست خاموش است هنوز «فعال» نشان داده می‌شود.
8. **تاریخچه TTL ندارد.** `price:history:<p>:<id>` هر کدام به ۱۰۰۰ رکورد محدودند
   ولی خود کلیدها هیچ‌وقت نمی‌میرند؛ با آمدن و رفتن آیتم‌ها تعدادشان بی‌نهایت
   رشد می‌کند.
9. **دو TTL متفاوت برای یک مفهوم:** `setCurrentPriceWithKey` شصت ثانیه،
   `setCurrentPrice` یک ساعت.

## ۳. چیدمان تازه — برای هر مفهوم، یک خانه

| کلید | نوع | محتوا |
| --- | --- | --- |
| `provider:<key>:items` | HASH | `itemId → متادیتای نماد`. **یکجا نوشته می‌شود** (DEL + HSET در یک pipeline)، پس همیشه یک مجموعه‌ی سازگار است و نمادِ حذف‌شده واقعاً حذف می‌شود |
| `provider:<key>:prices` | HASH | `itemId → آخرین قیمت`. جای هر سه نمایش قبلی |
| `provider:<key>:history:<itemId>` | ZSET | تاریخچه، سقف ۱۰۰۰ رکورد، **با TTL** |
| `providers:active` | SET | کلید تأمین‌کننده‌هایی که داده دارند — تا هیچ‌کس مجبور به `KEYS` نباشد |

سه خاصیتی که این چیدمان می‌دهد و قبلی نمی‌داد:

- **خواندن فهرست آیتم‌ها یک `HGETALL` است**، نه یک اسکن روی کل keyspace.
- **فهرست، اتمیک است.** یا کل مجموعه‌ی تازه است یا کل مجموعه‌ی قبلی؛ حالت
  «نصفش منقضی شده» وجود ندارد.
- **انقضا روی یک کلید است، نه روی تک‌تک اعضا.** یعنی یا تأمین‌کننده داده دارد یا
  ندارد — نه اینکه نصف نمادهایش باشد.

## ۴. صفحه‌ی مقایسه — چه چیزی غلط نمایش داده می‌شود

1. **ستون «آیتم» شناسه‌ی خام را نشان می‌دهد** (`s.providerItemId`). خود همین فایل
   تابع `itemOptionLabel` را دارد که برای همین ساخته شده، ولی سری‌های مقایسه اصلاً
   نام را حمل نمی‌کنند. اپراتور یک عدد می‌بیند.
2. **«تأمین‌کنندگان فعال»** از همان SET هرس‌نشده می‌آید (بند ۷) — تأمین‌کننده‌ی
   خاموش هم سبز نشان داده می‌شود.
3. **`useBuyPrice`/`useSellPrice` گرفته می‌شوند ولی نمایش داده نمی‌شوند.** جدول
   برای همه هم خرید و هم فروش را نشان می‌دهد، حتی وقتی نگاشت می‌گوید فقط یک طرفش
   استفاده می‌شود. یعنی عددی نشان داده می‌شود که گلدکس از آن تأمین‌کننده
   نمی‌خواند.
4. `METRIC_LABEL` تعریف شده و **هیچ‌جا استفاده نمی‌شود**؛ رشته‌هایش در خود
   `<select>` تکرار شده‌اند.

## ۵. جابه‌جایی

داده‌ها همگی مشتق و زودگذرند: قیمت‌ها هر چند ثانیه تازه می‌شوند و متادیتا روی
`metadataRefreshIntervalMs` (پیش‌فرض ۶۰ ثانیه) و در هر اتصال. پس **مهاجرت لازم
نیست** — کافی است موتور و بک‌اند با هم دیپلوی شوند. در فاصله‌ی کوتاه بعد از
دیپلوی، پنل تا اولین چرخه‌ی متادیتا خالی است.

کلیدهای قدیمی (`price:current:*`, `price:snapshot:*`, `item:metadata:*`) خودشان
با TTL می‌میرند؛ چیزی برای پاک کردن دستی نیست.

## ۶. فازها

| فاز | کار |
| --- | --- |
| ۱ ✅ | چیدمان تازه در موتور + ایندکس `providers:active` |
| ۲ ✅ | خواننده‌ی بک‌اند روی چیدمان تازه، و «فعال» یعنی واقعاً داده‌ی تازه دارد |
| ۳ ✅ | صفحه‌ی مقایسه: نام آیتم، سمت استفاده‌شده‌ی نگاشت، و وضعیت درست تأمین‌کننده |
| ۴ ✅ | تست‌ها |

## ۷. آنچه انجام شد

- `src/redis/keys.ts` در موتور: تنها جایی که شکل کلیدها تعریف می‌شود.
- `KEYS` از هر دو سرویس حذف شد. تست ذخیره‌سازی، fake Redis‌اش روی `keys()`
  **exception پرتاب می‌کند** — پس اگر کسی دوباره اسکن اضافه کند، تست می‌شکند.
- کانال‌های pub/sub (`price:updates` و `price:snapshot:<provider>`) عمداً **دست
  نخوردند**، چون مصرف‌کننده‌ی بیرون از این دو سرویس دارند.
- صفحه‌ی مقایسه: نام آیتم روی نمودار و در جدول، ستون «آخرین داده»، و
  سمتی از قیمت که نگاشت استفاده نمی‌کند به‌جای عدد، خط تیره نشان می‌دهد.
- نشان‌گر تأمین‌کننده سه حالت شد: نگاشت‌نشده / نگاشت‌شده ولی بی‌داده / در حال
  گزارش. قبلاً دو حالت بود و «نگاشت‌شده ولی ساکت» سبز نشان داده می‌شد.

**تست‌ها**: ۱۶ تست تازه. موتور ۱۱۴ سبز، بک‌اند ۸۵۲ سبز، پنل ۲۰۲ سبز.

## ۸. نیمه‌ی گمشده: چرا یک تأمین‌کننده فقط آیتم داشت و دیگری فقط قیمت

بعد از چیدمان تازه، در سرور واقعی دیده شد که در Redis فقط متادیتای `afrogh`
هست و فقط قیمت `arianatala` و `mirrokni`. لاگ کانتینر نشان داد این **دو اشکال
جدا** است، نه یک اشکال در چیدمان:

**۱) زریار (arianatala، mirrokni) — متادیتا خالی برمی‌گشت بدون خطا.**
درخواست `POST /api/Home/ShopkeeperItemsList` موفق بود (هیچ خط
`Failed to fetch metadata` در لاگ نبود) ولی `response.data.Data` چیزی
تولید نمی‌کرد و تنها چیزی که لاگ می‌گفت `No metadata found` بود — جمله‌ای که
به همه‌ی علت‌ها می‌خورد و هیچ‌کدام را مشخص نمی‌کند. قیمت‌ها سالم می‌آمدند چون
استریم SignalR مسیر جداگانه‌ای دارد و وقتی فهرست آیتم خالی است فیلتر
`shouldTrackItem` همه را عبور می‌دهد.

- خواندن پاسخ به `src/real-time-provider/providers/zaryar-items-response.ts`
  منتقل شد: یا آیتم می‌دهد، یا **جمله‌ای می‌دهد که می‌گوید در بدنه چه بود** —
  `Data` غایب/`null`/لیست خالی، گروه‌های بدون آیتم، یا بدنه‌ای که اصلاً JSON
  شیء نیست.
- پاکت `IsSuccess: false` دیگر بی‌صدا رد نمی‌شود؛ `Message` در لاگ می‌آید، و
  اگر پیام یا `StatusCode` از رد شدن **نشست** بگوید، مثل ۴۰۱ رفتار می‌شود و
  رویداد `PROVIDER_AUTH_EXPIRED` منتشر می‌گردد تا ورود دوباره راه بیفتد.
- شکل‌های دیگری که از یک بک‌اند .NET می‌آید هم پذیرفته می‌شود: `Data.Groups`،
  یک گروه بدون آرایه، و لیست تخت آیتم‌ها (که دوباره بر اساس `GroupName` خودش
  گروه‌بندی می‌شود).
- `fetchInitialPrices()` نوشته شده بود ولی **هیچ‌جا صدا زده نمی‌شد**؛ حالا در
  `connect()` صدا زده می‌شود، پس شاپی که در ساعت خلوت وصل شود دیگر با فهرست
  آیتم و بدون قیمت نمی‌ماند.

**۲) طلاآب (afrogh) — قیمتی نمی‌رسید، در حالی که «مشترک» شده بود.**
لاگ می‌گفت `Subscribed to afrogh channel`، ولی این خط **درست بعد از فرستادن**
فریم `pusher:subscribe` نوشته می‌شد، نه بعد از پاسخ سرور. یعنی کانالی که سرور
رد کرده بود عیناً شبیه کانالی بود که حرفی برای گفتن ندارد؛ و
`handleWebSocketMessage` هر فریمی را که نمی‌شناخت بی‌صدا دور می‌ریخت — از جمله
`pusher:error` و `pusher_internal:subscription_error`.

- `src/real-time-provider/providers/pusher-frames.ts` هر فریم را دسته‌بندی
  می‌کند؛ لاگ فقط وقتی «تأیید شد» می‌گوید که
  `pusher_internal:subscription_succeeded` رسیده باشد.
- رد شدن اشتراک با وضعیت ۴۰۱/۴۰۳ و خطای Pusher با کد ۴۰۰۱/۴۰۰۳/۴۰۰۹ یعنی
  توکن مرده: `PROVIDER_AUTH_EXPIRED` منتشر می‌شود. بقیه فقط خطای لاگ‌اند.
- اگر شاپ مشترک باشد و ۱۲۰ ثانیه هیچ فریم قیمتی نیاید، یک هشدار می‌آید که
  می‌گوید اشتراک تأیید شده بود یا نه.
- اگر فریم قیمتی برسد و **هیچ‌کدام** از شناسه‌هایش در فهرست آیتم نباشد، هشدار
  می‌دهد؛ قبلاً همه‌شان بی‌صدا دور ریخته می‌شد.
- نام کانال از `auth.channel` یا کلید تأمین‌کننده خوانده می‌شود؛ قبلاً
  `'afrogh'` در کد ثابت بود و شاپ دوم طلاآب اصلاً کار نمی‌کرد.
- mock در `mock-server/` هم حالا مثل Pusher واقعی اشتراک را تأیید می‌کند.

**تست‌ها**: ۲۷ تست تازه (موتور ۱۴۵ سبز).

آنچه بعد از دیپلوی در لاگ باید دید: به‌جای `No metadata found` یک جمله‌ی
`Item list unusable for metadata: …` که علت را می‌گوید، و برای طلاآب یا
`Subscription confirmed for afrogh` یا خطای رد شدن اشتراک. اگر جمله‌ی زریار
از `IsSuccess=false` بگوید، مشکل نشست است و ورود دوباره لازم است؛ اگر از شکل
بدنه بگوید، همان جمله می‌گوید کدام میدان عوض شده.
