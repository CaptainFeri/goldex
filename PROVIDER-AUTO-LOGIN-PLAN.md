# طرح ورود خودکار تأمین‌کننده از روی گوشی

## ۱. خواسته

روی گوشی‌ای که سیم‌کارت ثبت‌شده نزد تأمین‌کننده در آن است، اپ با اجازه‌ی مالک
پیامک‌های ورودی را بخواند؛ و هر وقت ارتباط یکی از تأمین‌کننده‌ها قطع شد،
خودش در پس‌زمینه ورود را شروع کند، کد پیامکی را بگیرد، تأیید را کامل کند و
نتیجه را به سرور بدهد — بدون اینکه کسی گوشی را دست بگیرد.

## ۲. آنچه امروز هست

| تکه | وضعیت |
| --- | --- |
| خواندن کد از پیامک و اعلان | هست (`OtpExtractor`، دو خواننده، تست‌دار) |
| رله‌ی کد به سرور | هست (`relay-otp` / `awaiting-otp`) |
| فعال‌سازی دستی از اپ و پنل | هست |
| تلاش مجدد اتصال در موتور | هست (`base-realtime.provider.ts`: backoff نمایی تا `maxReconnect`، و `ProviderManager` هر ۳۰ ثانیه دوباره `startProvider` صدا می‌زند) |
| رویداد قطع ارتباط | هست (`provider.disconnected` → `ProviderStatusConsumer` → `status` در آینه) |
| ذخیره‌ی credentials بعد از تأیید کد | **کار سرور است**، نه اپ |

نکته‌ی آخر مهم است و کار را کوچک می‌کند: `verifyOtp` در موتور خودش توکن را
می‌گیرد، در `auth` می‌نویسد، `active` را روشن می‌کند و provider را استارت
می‌زند. پس اپ «اطلاعات لازم» جداگانه‌ای برای فرستادن ندارد — تنها چیزی که باید
بفرستد **کد** است.

## ۳. دو مانعی که قبل از هر کاری باید برداشته شوند

این دو، تفاوت بین «کار می‌کند» و «به‌نظر کار می‌کند» هستند.

### مانع ۱ — موتور «نشست منقضی» را از «قطعی شبکه» تشخیص نمی‌دهد

در کل موتور هیچ‌جا ۴۰۱ بررسی نمی‌شود. `handleDisconnect` هر قطعی را یکسان
می‌بیند و تا ابد دوباره وصل می‌شود. اگر ورود خودکار را به رویداد
`provider.disconnected` وصل کنیم، نتیجه این است:

> با هر نوسان شبکه یا هر بار ری‌استارت موتور، یک ورود جدید شروع می‌شود، یک
> پیامک از تأمین‌کننده خواسته می‌شود، و سهمیه‌ی پیامک آن حساب می‌سوزد.

بعضی تأمین‌کننده‌ها روی تعداد درخواست کد سقت‌گیرند و حساب را موقتاً می‌بندند.
پس نقطه‌ی شروع طبقه‌بندی خطاست، نه اپ:

- `fetchAndStoreMetadata` و `getShopProfile` با توکن درخواست می‌زنند. پاسخ
  ۴۰۱/۴۰۳ آن‌ها **همان سیگنالی است که می‌خواهیم** و امروز بی‌صدا بلعیده می‌شود.
- رویداد تازه‌ای لازم است: `provider.auth.expired` با `{ key, at, reason }` —
  جدا از `provider.disconnected`، چون معنای‌شان یکی نیست.

### مانع ۲ — مسیر OTP فقط برای «اولین» فعال‌سازی نوشته شده

در `goldex-pricing-engine/src/real-time-provider/provider.service.ts`:

```ts
async sendOtp(...)   { if (provider.active) throw new BadRequestException('Provider is already active; cannot send OTP'); ... }
async verifyOtp(...) { if (provider.active) throw new BadRequestException('Provider is already active'); ... }
```

تأمین‌کننده‌ای که نشستش منقضی شده هنوز `active: true` است (این فلگ «باید روشن
باشد» را می‌گوید، نه «وصل است»). یعنی ورود مجدد خودکار **امروز از همان قدم اول
رد می‌شود**.

بدتر: `sendOtp` پیش از ارسال، `auth.token` را پاک می‌کند. اگر ورود مجدد شکست
بخورد، provider بدون هیچ توکنی می‌ماند — وضعیتی بدتر از قبل از تلاش.

راه درست: نشست تازه تا وقتی ثابت نشده، نشست قبلی را از بین نبرد. یعنی
`sendOtp` توکن موجود را پاک نکند و کد تازه در یک فیلد موقت بنشیند، و فقط
`verifyOtp` موفق آن را جابه‌جا کند.

## ۴. جریان پیشنهادی

```
موتور: connect → درخواست metadata با توکن → 401
   └→ publish provider.auth.expired {key}
بک‌اند: consumer → status = 'auth_expired' + ثبت در صف «نیازمند ورود»
   └→ اعلام به دستگاه‌های ثبت‌شده
گوشی: lease می‌گیرد (قفل سمت سرور)
   → POST /admin/providers/:id/send-otp
   → پیامک می‌رسد → OtpExtractor
   → POST /admin/providers/:id/verify-otp
بک‌اند/موتور: توکن تازه ذخیره، provider استارت
   └→ lease آزاد، رویداد ثبت (چه کسی، چه ساعتی، خودکار)
```

### کانال اعلان به گوشی

دو گزینه، و باید یکی انتخاب شود:

1. **socket.io** روی همان gateway ادمین که هست (`admin-notifications`,
   با احراز JWT ادمین). سریع و بدون poll، اما نیازمند نگه‌داشتن یک سوکت زنده
   در پس‌زمینه‌ی اندروید — که خودش یعنی foreground service.
2. **poll هر ۳۰ تا ۶۰ ثانیه** روی یک endpoint سبک. ساده‌تر، بدون زیرساخت تازه،
   و برای دستگاهی که کارش همین است، هزینه‌ی باتری‌اش قابل قبول است.

پیشنهاد: **گزینه‌ی ۲ برای شروع.** تأخیر نیم‌دقیقه‌ای در برابر نشستی که ساعت‌ها
منقضی مانده بی‌اهمیت است، و یک سوکت زنده روی اندروید مسئله‌ی خودش را دارد
(Doze، کشته‌شدن سرویس، اتصال‌های نیمه‌مرده).

## ۵. قفل و سهمیه — بدون این‌ها ساخته نشود

- **lease سمت سرور**: `provider:autologin:lease:<key>` با مالک (شناسه‌ی دستگاه)
  و TTL حدود ۳ دقیقه. دو گوشی یا دو نصب نباید هم‌زمان کد بخواهند.
- **احترام به انسان**: اگر `awaiting-otp` نشان می‌دهد کسی در پنل وسط فعال‌سازی
  است، دستگاه دست نگه دارد. کد یک‌بارمصرف است و دو درخواست یعنی هر دو می‌سوزند.
- **cooldown**: بعد از هر شکست، فاصله‌ی فزاینده (مثلاً ۵ → ۱۵ → ۶۰ دقیقه).
- **سقف**: بعد از ۳ شکست پیاپی، ورود خودکار برای آن تأمین‌کننده متوقف و به
  اپراتور اعلام شود. تلاش بی‌پایان همان چیزی است که حساب را می‌بندد.
- **سقف روزانه** برای هر تأمین‌کننده، مستقل از شکست و موفقیت.

شمارنده‌ها همه سمت سرور، نه روی گوشی: ری‌استارت گوشی نباید سقف را صفر کند.

## ۶. واقعیت‌های اندروید

- کار در پس‌زمینه با تأخیر کوتاه یعنی **foreground service** با نوتیف دائمی
  (`FOREGROUND_SERVICE_DATA_SYNC`). بدون آن، `WorkManager` دوره‌ای حداقل
  ۱۵ دقیقه است و در Doze بیشتر.
- **معافیت از بهینه‌سازی باتری** باید از کاربر گرفته شود، وگرنه سرویس خوابانده
  می‌شود.
- سازنده‌های چینی (شیائومی، هواوی…) سرویس‌ها را جدای از Doze می‌کشند؛ اپ باید
  بتواند بگوید «آخرین بار کِی زنده بودم» تا اپراتور بفهمد گوشی خوابیده.
- `RECEIVE_SMS` در زمان اجرا از مالک گرفته می‌شود (الان هم همین‌طور است).
- گوشی باید همانی باشد که سیم‌کارت تأمین‌کننده در آن است؛ اگر چند تأمین‌کننده
  چند سیم‌کارت دارند، یا چند گوشی لازم است یا گوشی دوسیم‌کارته و تشخیص
  `subscriptionId` پیامک.

## ۷. امنیت — این بخش را جدی بگیرید

ورود خودکار یعنی یک گوشی که **بدون ناظر** می‌تواند تأمین‌کننده فعال کند.

- امروز اپ **توکن کامل ادمین** را نگه می‌دارد. برای کار بی‌ناظر یعنی گوشی‌ای در
  جیب کسی با دسترسی کامل ادمین. پیشنهاد: اعتبارنامه‌ی مخصوص دستگاه با دامنه‌ی
  محدود (فقط `send-otp`, `verify-otp`, `relay-otp`, `awaiting-otp` و خواندن
  فهرست تأمین‌کننده‌ها)، قابل ابطال از پنل، با ثبت دستگاه.
- **یافته‌ی جانبی که باید بدانید**: در `admin.module.ts` مقدار انقضا با
  `Number(...)` خوانده می‌شود و مقدار نمونه در `.env.example` رشته‌ی `1d` است.
  `Number('1d')` برابر `NaN` است، یعنی توکن‌ها عملاً بدون انقضا صادر می‌شوند.
  این باید اصلاح شود — اما توجه کنید که **اصلاحش دستگاه بی‌ناظر را از کار
  می‌اندازد**، چون توکنش منقضی خواهد شد. پس یا اعتبارنامه‌ی دستگاه (بالا) یا
  مکانیزم refresh لازم است. این هم یک دلیل دیگر برای جدا کردن اعتبارنامه‌ی
  دستگاه از نشست ادمین.
- هر ورود خودکار باید **audit** شود: کدام دستگاه، چه ساعتی، موفق یا ناموفق.
  کاری که کسی ندیده انجام شود، باید جایی نوشته شده باشد.

## ۸. فازبندی

| فاز | کار | چرا این ترتیب |
| --- | --- | --- |
| ۰ | تشخیص ۴۰۱/۴۰۳ در موتور + رویداد `provider.auth.expired` | بدون آن، ماشه‌ای وجود ندارد جز «هر قطعی»، که غلط است |
| ۱ | برداشتن گارد `active` از مسیر ورود مجدد + حفظ توکن قبلی تا اثبات توکن تازه | بدون آن، ورود مجدد از قدم اول رد می‌شود |
| ۲ | سمت سرور: وضعیت `auth_expired`، صف، lease، cooldown، سقف، endpointها | قفل و سهمیه پیش از خودکارسازی، نه بعد از آن |
| ۳ | اعتبارنامه و ثبت دستگاه | پیش از آنکه گوشی بدون ناظر کار کند |
| ۴ | اپ: foreground service + ماشین حالت ورود خودکار | حالا همه‌چیز برای این آماده است |
| ۵ | دیده‌بانی: نمایش در پنل، audit، اعلان شکست به اپراتور | خودکارسازیِ نادیدنی، خرابیِ نادیدنی می‌سازد |
| ۶ | تست‌ها | ماشین حالت، cooldown، lease، و طبقه‌بندی ۴۰۱ |

## ۹. ریسک‌ها

- **پیامک دیر یا اصلاً نرسد** → ماشین حالت باید timeout داشته باشد (مثلاً ۲
  دقیقه) و بعد از آن تلاش را شکست‌خورده اعلام کند، نه اینکه معلق بماند.
- **کد اشتباه از پیام دیگر** → `OtpExtractor` امتیازدهی می‌کند، اما در حالت
  بی‌ناظر کسی نیست که ببیند. پیشنهاد: در ورود خودکار فقط کدی پذیرفته شود که
  پیامش «برچسب کد» دارد (امتیاز صفر کافی نباشد).
- **تأمین‌کننده‌ی دارای کپچا** → اصلاً از این مسیر فعال نمی‌شود. باید صریحاً از
  ورود خودکار کنار گذاشته شود، نه اینکه تا سقف تلاش برود.
- **چند دستگاه با یک سیم‌کارت شماره‌گذاری‌شده** → lease حل می‌کند، اما باید
  تست شود.
- **حلقه‌ی خودکار در برابر تأمین‌کننده‌ای که خودش قطع و وصل می‌شود** →
  cooldown و سقف روزانه تنها چیزی است که جلویش را می‌گیرد.

## ۱۰. آنچه از شما لازم است

1. کانال اعلان: poll (پیشنهاد) یا socket؟
2. ورود خودکار پیش‌فرض برای همه روشن باشد یا فلگ per-provider؟
3. سقف تلاش و cooldown چه اعدادی؟
4. اعتبارنامه‌ی مخصوص دستگاه بسازیم یا فعلاً همان توکن ادمین بماند؟
