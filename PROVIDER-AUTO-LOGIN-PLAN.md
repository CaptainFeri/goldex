# طرح ورود خودکار تأمین‌کننده از روی گوشی

## ۱. خواسته

روی گوشی‌ای که سیم‌کارت ثبت‌شده نزد تأمین‌کننده در آن است، اپ با اجازه‌ی مالک
پیامک‌های ورودی را بخواند؛ و هر وقت ارتباط یکی از تأمین‌کننده‌ها قطع شد،
خودش در پس‌زمینه ورود را شروع کند، کد پیامکی را بگیرد، تأیید را کامل کند و
نتیجه را به سرور بدهد — بدون اینکه کسی گوشی را دست بگیرد.

## ۲. آنچه امروز هست

| تکه | وضعیت |
| --- | --- |
| خواندن کد از پیامک و اعلان | هست (`OtpExtractor`، دو خواننده، تست‌دار) |
| رله‌ی کد به سرور | هست (`relay-otp` / `awaiting-otp`) |
| فعال‌سازی دستی از اپ و پنل | هست |
| تلاش مجدد اتصال در موتور | هست (`base-realtime.provider.ts`: backoff نمایی تا `maxReconnect`، و `ProviderManager` هر ۳۰ ثانیه دوباره `startProvider` صدا می‌زند) |
| رویداد قطع ارتباط | هست (`provider.disconnected` → `ProviderStatusConsumer` → `status` در آینه) |
| ذخیره‌ی credentials بعد از تأیید کد | **کار سرور است**، نه اپ |

نکته‌ی آخر مهم است و کار را کوچک می‌کند: `verifyOtp` در موتور خودش توکن را
می‌گیرد، در `auth` می‌نویسد، `active` را روشن می‌کند و provider را استارت
می‌زند. پس اپ «اطلاعات لازم» جداگانه‌ای برای فرستادن ندارد — تنها چیزی که باید
بفرستد **کد** است.

## ۳. دو مانعی که قبل از هر کاری باید برداشته شوند

این دو، تفاوت بین «کار می‌کند» و «به‌نظر کار می‌کند» هستند.

### مانع ۱ — موتور «نشست منقضی» را از «قطعی شبکه» تشخیص نمی‌دهد

در کل موتور هیچ‌جا ۴۰۱ بررسی نمی‌شود. `handleDisconnect` هر قطعی را یکسان
می‌بیند و تا ابد دوباره وصل می‌شود. اگر ورود خودکار را به رویداد
`provider.disconnected` وصل کنیم، نتیجه این است:

> با هر نوسان شبکه یا هر بار ری‌استارت موتور، یک ورود جدید شروع می‌شود، یک
> پیامک از تأمین‌کننده خواسته می‌شود، و سهمیه‌ی پیامک آن حساب می‌سوزد.

بعضی تأمین‌کننده‌ها روی تعداد درخواست کد سقت‌گیرند و حساب را موقتاً می‌بندند.
پس نقطه‌ی شروع طبقه‌بندی خطاست، نه اپ:

- `fetchAndStoreMetadata` و `getShopProfile` با توکن درخواست می‌زنند. پاسخ
  ۴۰۱/۴۰۳ آن‌ها **همان سیگنالی است که می‌خواهیم** و امروز بی‌صدا بلعیده می‌شود.
- رویداد تازه‌ای لازم است: `provider.auth.expired` با `{ key, at, reason }` —
  جدا از `provider.disconnected`، چون معنای‌شان یکی نیست.

### مانع ۲ — مسیر OTP فقط برای «اولین» فعال‌سازی نوشته شده

در `goldex-pricing-engine/src/real-time-provider/provider.service.ts`:

```ts
async sendOtp(...)   { if (provider.active) throw new BadRequestException('Provider is already active; cannot send OTP'); ... }
async verifyOtp(...) { if (provider.active) throw new BadRequestException('Provider is already active'); ... }
```

تأمین‌کننده‌ای که نشستش منقضی شده هنوز `active: true` است (این فلگ «باید روشن
باشد» را می‌گوید، نه «وصل است»). یعنی ورود مجدد خودکار **امروز از همان قدم اول
رد می‌شود**.

بدتر: `sendOtp` پیش از ارسال، `auth.token` را پاک می‌کند. اگر ورود مجدد شکست
بخورد، provider بدون هیچ توکنی می‌ماند — وضعیتی بدتر از قبل از تلاش.

راه درست: نشست تازه تا وقتی ثابت نشده، نشست قبلی را از بین نبرد. یعنی
`sendOtp` توکن موجود را پاک نکند و کد تازه در یک فیلد موقت بنشیند، و فقط
`verifyOtp` موفق آن را جابه‌جا کند.

## ۴. جریان پیشنهادی

```
موتور: connect → درخواست metadata با توکن → 401
   └→ publish provider.auth.expired {key}
بک‌اند: consumer → status = 'auth_expired' + ثبت در صف «نیازمند ورود»
   └→ اعلام به دستگاه‌های ثبت‌شده
گوشی: lease می‌گیرد (قفل سمت سرور)
   → POST /admin/providers/:id/send-otp
   → پیامک می‌رسد → OtpExtractor
   → POST /admin/providers/:id/verify-otp
بک‌اند/موتور: توکن تازه ذخیره، provider استارت
   └→ lease آزاد، رویداد ثبت (چه کسی، چه ساعتی، خودکار)
```

### کانال اعلان به گوشی

دو گزینه، و باید یکی انتخاب شود:

1. **socket.io** روی همان gateway ادمین که هست (`admin-notifications`,
   با احراز JWT ادمین). سریع و بدون poll، اما نیازمند نگه‌داشتن یک سوکت زنده
   در پس‌زمینه‌ی اندروید — که خودش یعنی foreground service.
2. **poll هر ۳۰ تا ۶۰ ثانیه** روی یک endpoint سبک. ساده‌تر، بدون زیرساخت تازه،
   و برای دستگاهی که کارش همین است، هزینه‌ی باتری‌اش قابل قبول است.

پیشنهاد: **گزینه‌ی ۲ برای شروع.** تأخیر نیم‌دقیقه‌ای در برابر نشستی که ساعت‌ها
منقضی مانده بی‌اهمیت است، و یک سوکت زنده روی اندروید مسئله‌ی خودش را دارد
(Doze، کشته‌شدن سرویس، اتصال‌های نیمه‌مرده).

## ۵. قفل و سهمیه — بدون این‌ها ساخته نشود

- **lease سمت سرور**: `provider:autologin:lease:<key>` با مالک (شناسه‌ی دستگاه)
  و TTL حدود ۳ دقیقه. دو گوشی یا دو نصب نباید هم‌زمان کد بخواهند.
- **احترام به انسان**: اگر `awaiting-otp` نشان می‌دهد کسی در پنل وسط فعال‌سازی
  است، دستگاه دست نگه دارد. کد یک‌بارمصرف است و دو درخواست یعنی هر دو می‌سوزند.
- **cooldown**: بعد از هر شکست، فاصله‌ی فزاینده (مثلاً ۵ → ۱۵ → ۶۰ دقیقه).
- **سقف**: بعد از ۳ شکست پیاپی، ورود خودکار برای آن تأمین‌کننده متوقف و به
  اپراتور اعلام شود. تلاش بی‌پایان همان چیزی است که حساب را می‌بندد.
- **سقف روزانه** برای هر تأمین‌کننده، مستقل از شکست و موفقیت.

شمارنده‌ها همه سمت سرور، نه روی گوشی: ری‌استارت گوشی نباید سقف را صفر کند.

## ۶. واقعیت‌های اندروید

- کار در پس‌زمینه با تأخیر کوتاه یعنی **foreground service** با نوتیف دائمی
  (`FOREGROUND_SERVICE_DATA_SYNC`). بدون آن، `WorkManager` دوره‌ای حداقل
  ۱۵ دقیقه است و در Doze بیشتر.
- **معافیت از بهینه‌سازی باتری** باید از کاربر گرفته شود، وگرنه سرویس خوابانده
  می‌شود.
- سازنده‌های چینی (شیائومی، هواوی…) سرویس‌ها را جدای از Doze می‌کشند؛ اپ باید
  بتواند بگوید «آخرین بار کِی زنده بودم» تا اپراتور بفهمد گوشی خوابیده.
- `RECEIVE_SMS` در زمان اجرا از مالک گرفته می‌شود (الان هم همین‌طور است).
- گوشی باید همانی باشد که سیم‌کارت تأمین‌کننده در آن است؛ اگر چند تأمین‌کننده
  چند سیم‌کارت دارند، یا چند گوشی لازم است یا گوشی دوسیم‌کارته و تشخیص
  `subscriptionId` پیامک.

## ۷. امنیت — این بخش را جدی بگیرید

ورود خودکار یعنی یک گوشی که **بدون ناظر** می‌تواند تأمین‌کننده فعال کند.

- امروز اپ **توکن کامل ادمین** را نگه می‌دارد. برای کار بی‌ناظر یعنی گوشی‌ای در
  جیب کسی با دسترسی کامل ادمین. پیشنهاد: اعتبارنامه‌ی مخصوص دستگاه با دامنه‌ی
  محدود (فقط `send-otp`, `verify-otp`, `relay-otp`, `awaiting-otp` و خواندن
  فهرست تأمین‌کننده‌ها)، قابل ابطال از پنل، با ثبت دستگاه.
- **یافته‌ی جانبی که باید بدانید**: در `admin.module.ts` مقدار انقضا با
  `Number(...)` خوانده می‌شود و مقدار نمونه در `.env.example` رشته‌ی `1d` است.
  `Number('1d')` برابر `NaN` است، یعنی توکن‌ها عملاً بدون انقضا صادر می‌شوند.
  این باید اصلاح شود — اما توجه کنید که **اصلاحش دستگاه بی‌ناظر را از کار
  می‌اندازد**، چون توکنش منقضی خواهد شد. پس یا اعتبارنامه‌ی دستگاه (بالا) یا
  مکانیزم refresh لازم است. این هم یک دلیل دیگر برای جدا کردن اعتبارنامه‌ی
  دستگاه از نشست ادمین.
- هر ورود خودکار باید **audit** شود: کدام دستگاه، چه ساعتی، موفق یا ناموفق.
  کاری که کسی ندیده انجام شود، باید جایی نوشته شده باشد.

## ۸. فازبندی

| فاز | کار | چرا این ترتیب |
| --- | --- | --- |
| ۰ ✅ | تشخیص ۴۰۱/۴۰۳ در موتور + رویداد `provider.auth.expired` | بدون آن، ماشه‌ای وجود ندارد جز «هر قطعی»، که غلط است |
| ۱ ✅ | برداشتن گارد `active` از مسیر ورود مجدد + حفظ توکن قبلی تا اثبات توکن تازه | بدون آن، ورود مجدد از قدم اول رد می‌شود |
| ۲ ✅ | سمت سرور: وضعیت `auth_expired`، صف، lease، cooldown، سقف، endpointها | قفل و سهمیه پیش از خودکارسازی، نه بعد از آن |
| ۳ ✅ | اعتبارنامه و ثبت دستگاه | پیش از آنکه گوشی بدون ناظر کار کند |
| ۴ ✅ | اپ: foreground service + ماشین حالت ورود خودکار | حالا همه‌چیز برای این آماده است |
| ۵ ✅ | دیده‌بانی: نمایش در پنل، audit، اعلان شکست به اپراتور | خودکارسازیِ نادیدنی، خرابیِ نادیدنی می‌سازد |
| ۶ ✅ | تست‌ها | ماشین حالت، cooldown، lease، و طبقه‌بندی ۴۰۱ |

## ۹. ریسک‌ها

- **پیامک دیر یا اصلاً نرسد** → ماشین حالت باید timeout داشته باشد (مثلاً ۲
  دقیقه) و بعد از آن تلاش را شکست‌خورده اعلام کند، نه اینکه معلق بماند.
- **کد اشتباه از پیام دیگر** → `OtpExtractor` امتیازدهی می‌کند، اما در حالت
  بی‌ناظر کسی نیست که ببیند. پیشنهاد: در ورود خودکار فقط کدی پذیرفته شود که
  پیامش «برچسب کد» دارد (امتیاز صفر کافی نباشد).
- **تأمین‌کننده‌ی دارای کپچا** → اصلاً از این مسیر فعال نمی‌شود. باید صریحاً از
  ورود خودکار کنار گذاشته شود، نه اینکه تا سقف تلاش برود.
- **چند دستگاه با یک سیم‌کارت شماره‌گذاری‌شده** → lease حل می‌کند، اما باید
  تست شود.
- **حلقه‌ی خودکار در برابر تأمین‌کننده‌ای که خودش قطع و وصل می‌شود** →
  cooldown و سقف روزانه تنها چیزی است که جلویش را می‌گیرد.

## ۱۰. آنچه از شما لازم است

1. کانال اعلان: poll (پیشنهاد) یا socket؟
2. ورود خودکار پیش‌فرض برای همه روشن باشد یا فلگ per-provider؟
3. سقف تلاش و cooldown چه اعدادی؟
4. اعتبارنامه‌ی مخصوص دستگاه بسازیم یا فعلاً همان توکن ادمین بماند؟

---

## پیوست — آنچه در فاز ۰ و ۱ انجام شد

**فاز ۰ — تشخیص**

- `common/http/auth-failure.ts`: فقط ۴۰۱ و ۴۰۳ به‌عنوان «نشست رد شد» شمرده
  می‌شوند. عمداً تنگ: timeout، ECONNREFUSED و ۵۰۰ و ۴۲۹ هیچ نظری درباره‌ی نشست
  ندارند و شمردن‌شان یعنی درخواست کد تازه با هر نوسان شبکه.
- `BaseRealtimeProvider.reportAuthExpired` رویداد `provider.auth.expired` را
  **یک‌بار** منتشر می‌کند و تلاش مجدد را متوقف می‌کند.
- نکته‌ای که سر راه پیدا شد: `fetchMetadataFromApi` در هر دو provider خطا را
  می‌بلعید و `[]` برمی‌گرداند؛ یعنی ۴۰۱ هرگز به `connect()` نمی‌رسید و
  تأمین‌کننده با نشستِ مرده «متصل» گزارش می‌شد. حالا همان نقاط از
  `noteIfAuthFailure` رد می‌شوند و `startProvider` بعد از connect صریحاً
  می‌پرسد نشست رد شده یا نه.
- `ProviderManager` تأمین‌کننده‌ی نیازمند ورود را پارک می‌کند — نه با یک فلگ،
  بلکه با اثر انگشت همان نشستی که رد شده؛ به‌محض ذخیره‌ی نشست تازه خودبه‌خود
  آزاد می‌شود.
- بک‌اند رویداد را مصرف و `status = 'auth_expired'` ثبت می‌کند؛ پنل آن را
  «نیازمند ورود مجدد» نشان می‌دهد، نه «قطع».

**فاز ۱ — مسیر ورود مجدد**

- گارد `active` با `isServing` جایگزین شد: فقط تأمین‌کننده‌ای که **واقعاً متصل
  و در حال سرویس** است رد می‌شود. همان محافظتی که گارد قبلی می‌خواست بدهد، بدون
  اینکه ورود مجدد را غیرممکن کند.
- `sendOtp` دیگر توکن موجود را پاک نمی‌کند. نشست تازه در `verifyOtp` جایگزین
  می‌شود، یعنی یک ارسال ناموفق دیگر تأمین‌کننده را بدون هیچ نشستی رها نمی‌کند.
- `verifyOtp` نشست را **جایگزین** می‌کند نه ادغام؛ وگرنه `sessionId` قدیمی کنار
  توکن تازه می‌ماند و مجموعه‌ای می‌سازد که هرگز وجود نداشته.

**تست‌ها**: ۳۳ تست تازه (۸ طبقه‌بندی خطا، ۷ رفتار انقضا، ۴ پارک‌کردن،
۱۴ ورود مجدد). موتور ۹۸ سبز، بک‌اند ۵۱ سبز، پنل ۲۰۲ سبز.

**فاز ۲ — قفل و سهمیه (سمت سرور)**

- `ProviderAutoLoginService`: همه‌ی محدودیت‌ها سمت سرور، نه روی گوشی — گوشی با
  ری‌استارت شمارنده‌اش را فراموش می‌کند و دو گوشی از هم خبر ندارند.
- endpointها:
  - `GET /admin/providers/needs-login` — هر تأمین‌کننده‌ای که موتور گفته نشستش
    رد شده، همراه با `eligible` و `reason`. تأمین‌کننده‌ی در cooldown هم فهرست
    می‌شود؛ پنهان کردنش یعنی وانمود کردن که مشکل حل شده.
  - `POST /admin/providers/:id/login-lease` — گرفتن نوبت (۳ دقیقه)
  - `POST /admin/providers/:id/login-lease/release` — پس دادن با نتیجه
- **تلاش هنگام گرفتن نوبت شمرده می‌شود، نه در پایان.** گوشی‌ای که بین درخواست
  کد و استفاده از آن خاموش شود، وگرنه هیچ ردی نمی‌گذارد و بلافاصله می‌تواند
  دوباره تلاش کند — همان حلقه‌ای که این محدودیت‌ها برای جلوگیری از آن هستند.
- cooldown: ۵ سپس ۱۵ دقیقه؛ بعد از ۳ شکست پیاپی **متوقف** تا کسی نگاه کند.
  سقف روزانه ۶ تلاش مستقل از نتیجه.
- هر فعال‌سازی موفق — از گوشی یا از پنل یا با نشست دستی — شمارنده را صفر
  می‌کند: شمارشِ شکست در راه‌اندازی یک تأمین‌کننده، وقتی تأمین‌کننده کار
  می‌کند، چیزی را توصیف نمی‌کند.
- محدودیت‌ها فقط تلاش **خودکار** را می‌بندند. اپراتور پشت پنل هرگز پشت cooldown
  نمی‌ماند؛ سقف برای متوقف کردن حلقه‌ی بی‌ناظر است و آدمی که تصمیم می‌گیرد
  دوباره تلاش کند همان قضاوتی را کرده که cooldown جایش نشسته بود.

**تست‌ها**: ۲۰ تست تازه. بک‌اند ۸۱۰ سبز.

**فاز ۳ — اعتبارنامه‌ی دستگاه**

- جدول `provider_login_device`: فقط **هش SHA-256** توکن ذخیره می‌شود، نه خود آن.
  پس یک dump از دیتابیس، دسته‌کلید پلتفرم نیست. کار سنگین (bcrypt) لازم نیست چون
  راز ۲۵۶ بیت تصادفی است، نه رمز عبور — چیزی برای حدس زدن وجود ندارد.
- **سطح دسترسی به‌جای guard دو حالته:** مسیرهای دستگاه یک کنترلر جداگانه‌اند
  (`device/providers/*`) پشت `DeviceAuthGuard`. یعنی آنچه یک گوشی روی میز
  می‌تواند صدا بزند، در یک فایل خوانده می‌شود — نه زیرمجموعه‌ای از API ادمین که
  یک guard اتفاقاً اجازه‌اش می‌دهد.
- **شناسه‌ی دستگاه از اعتبارنامه خوانده می‌شود، نه از بدنه‌ی درخواست.** دو
  endpoint فاز ۲ که `deviceId` را از body می‌گرفتند حذف شدند: دستگاهی که بتواند
  خودش را نام‌گذاری کند، می‌تواند lease دستگاه دیگری را که هنوز منتظر کد است آزاد
  کند.
- `assertHolder` قبل از هر `send-otp`/`verify-otp` دستگاه بررسی می‌کند. بدون آن
  lease صرفاً توصیه‌ای بود و دستگاه می‌توانست مستقیم سراغ درخواست کد برود.
- ابطال: ردیف می‌ماند (سابقه‌ی اینکه دستگاه وجود داشته و عمل کرده، باید از اعتماد
  عمر بیشتری داشته باشد)، و `lastSeenAt` تنها چیزی است که می‌گوید گوشیِ بی‌ناظر
  هنوز زنده است.
- پنل: بخش «دستگاه‌های ورود خودکار» کنار تأمین‌کننده‌ها — افزودن، فهرست، ابطال،
  و نمایش توکن **فقط یک‌بار**.
- اپ: توکن دستگاه جدا از توکن ادمین نگه داشته می‌شود؛ **خروج از حساب آن را پاک
  نمی‌کند**، چون به گوشی داده شده نه به کسی که وارد شده. رله‌ی کد حالا با همین
  اعتبارنامه انجام می‌شود، یعنی با نبودن هیچ‌کس وارد‌شده هم کار می‌کند — چیزی که
  قبلاً هر کدی را دور می‌انداخت.

**تست‌ها**: ۱۹ تست تازه (۱۵ اعتبارنامه و guard، ۴ الزام دارنده‌ی lease).
بک‌اند ۸۲۹ سبز، پنل ۲۰۲ سبز.

**فاز ۴ — ورود خودکار روی گوشی**

- `AutoLoginEngine` عمداً **بدون هیچ import اندرویدی** نوشته شد: Retrofit و
  اعتبارنامه و آدرس سرور پشت یک interface مانده‌اند. چیزی که باقی می‌ماند خود
  توالی است — و هر شاخه‌ی آن راهی است برای هدر دادن سهمیه‌ی پیامک یک نفر دیگر،
  پس باید تست‌شدنی می‌بود. ۱۳ تست، همه اجرا شده.
- **یک تأمین‌کننده در هر پاس.** دو فعال‌سازی هم‌زمان یعنی دو کد با چند ثانیه
  فاصله روی یک گوشی و هیچ راهی برای تشخیص اینکه کدام برای کدام است — و هزینه‌ی
  اشتباه، سوختن هر دو.
- **استخراج سخت‌گیرانه در حالت بی‌ناظر**: فقط کدی پذیرفته می‌شود که پیام آن را
  «کد» نامیده باشد. با اپراتور پشت صفحه، حدسِ «تنها عدد پیام» ارزش نشان دادن
  دارد چون قابل بررسی است؛ بدون ناظر، کسی نیست که بررسی کند.
- پنجره‌ی انتظار کد **قبل** از ارسال درخواست باز می‌شود، چون روی شبکه‌ی سریع
  پیامک می‌تواند زودتر از پاسخ برسد.
- سرویس foreground با نوتیفیکیشن دائمی — قیمتی که اندروید برای کار قابل‌اتکا
  می‌گیرد و اینجا ارزشش را دارد: کار دوره‌ای در بهترین حالت ۱۵ دقیقه است و در
  Doze بیشتر. متن نوتیفیکیشن **نتیجه‌ی آخرین پاس** است، نه یک برچسب ثابت، چون
  تنها نشانه‌ی صادقانه‌ی زنده بودن گوشی همین است.
- سه راه برگشت به کار: `START_STICKY` برای وقتی سیستم سرویس را بکشد،
  `BootReceiver` برای ری‌استارت، و باز کردن اپ برای مرگ پروسه.
- معافیت از بهینه‌سازی باتری در تنظیمات — بدون آن اندروید سرویس را **بی‌صدا**
  می‌خواباند و اپراتور اپی می‌بیند که می‌گوید فعال است و تأمین‌کننده‌ای که پایین
  مانده.
- اعتبارنامه هر پاس بررسی می‌شود، نه یک‌بار در شروع: ابطال از پنل باید همان‌جا
  متوقفش کند، نه اینکه گوشی مدام رد شود.

**تست‌ها**: ۱۶ تست تازه (۱۳ ماشین حالت، ۳ استخراج سخت‌گیرانه) — روی JVM اجرا و سبز.

**فاز ۵ — دیده‌بانی**

- جدول `provider_login_attempt`: **عمداً جدا از شمارنده‌های Redis**. آن‌ها تصمیم
  می‌گیرند تلاش بعدی مجاز هست یا نه و بعد از شش ساعت فراموش می‌کنند تا شکست
  قدیمی جلوی تأمین‌کننده را نگیرد. فراموشی برای یک تنظیم‌کننده درست است و برای
  یک سند غلط: «چرا این تأمین‌کننده تمام شب پایین بود» سؤالی است که صبح پرسیده
  می‌شود.
- **ردیف هنگام شروع تلاش نوشته می‌شود، نه پایانش** — تلاش‌هایی که ارزش بررسی
  دارند دقیقاً همان‌هایی‌اند که هرگز تمام نشدند. گوشی‌ای که وسط کار خاموش شود
  وگرنه هیچ ردی نمی‌گذارد و اپراتور تأمین‌کننده‌ای می‌بیند که انگار اصلاً تلاش
  نشده.
- نام دستگاه در ردیف ذخیره می‌شود نه ارجاع به آن، تا ابطال دستگاه تاریخچه‌اش را
  ناخوانا نکند.
- **اعلان به اپراتور** در لحظه‌ای که سیستم خودکار تسلیم می‌شود (سقف ۳ تلاش) از
  همان gateway موجود ادمین. یک‌بار، نه در هر تلاش ناموفق — اعلانی که هر دقیقه
  بیاید اعلانی است که کسی نمی‌خواند.
- علت شکست را **خود گوشی** می‌فرستد، با کلمات خودش؛ پنل همان جمله را نشان
  می‌دهد و بازنویسی‌اش نمی‌کند — دو جا برای درست نگه داشتن یعنی آنکه روی صفحه
  است همانی است که از واقعیت دور می‌افتد.
- پنل: بخش «ورود خودکار تأمین‌کنندگان» — چه کسی منتظر است، چرا هنوز تلاش نشده،
  و آخرین ۱۵ تلاش با نتیجه و علت.

**تست‌ها**: ۶ تست تازه (۴ ثبت و اعلان، ۲ علت شکست). بک‌اند ۸۳۳ سبز، پنل ۲۰۲ سبز،
ماشین حالت ۱۵ سبز.

**فاز ۶ — تست‌های درز**

آنچه فازهای قبل نوشتند تست واحد بود و همه‌ی موارد این ردیف را پوشش می‌داد
(طبقه‌بندی ۴۰۱، cooldown، lease، ماشین حالت). چیزی که هیچ‌کدام نمی‌گرفتند،
**درز بین قطعه‌ها** بود — و دقیقاً همان‌جا بود که دو باگ واقعی این کار رخ داد.
پس فاز ۶ تست واحدِ بیشتر نشد، تست درز شد:

- `auto-login-routes.spec.ts`: یک اپلیکیشن Nest واقعی با supertest، ولی با
  دیتابیس و Redis و صف قلابی، تا در همان `jest` معمولی اجرا شود نه با Postgres.
  پوشش: ترتیب مسیرها (مسیر literal بعد از `:id` هرگز اجرا نمی‌شود — این باگ
  **دو بار** در همین کار رخ داد)، اینکه اعتبارنامه‌ی دستگاه API ادمین را باز
  **نمی‌کند**، الزام lease روی HTTP، و کل توالی claim → send → verify.
- **یک باگ واقعی پیدا کرد:** `verifyOtp` از طریق `noteActivated` خودش lease را
  پاک می‌کرد، و بعد `deviceVerifyOtp` دوباره release می‌زد و ۴۰۰ می‌گرفت. یعنی
  **هر ورود موفق دستگاه با خطا برمی‌گشت** در حالی که ورود انجام شده بود. هیچ تست
  واحدی این را نمی‌دید، چون هر دو طرف جداگانه درست بودند.
- `CodeToEngineTest.kt`: درز بین خواندن پیام و عمل کردن روی آن — کد بدون برچسب
  به تأمین‌کننده نمی‌رسد، کدِ تلاش قبلی جواب این تلاش نیست، و یک کد دوبار مصرف
  نمی‌شود.

**تست‌ها**: ۲۴ تست تازه (۱۹ مسیر، ۵ درز). بک‌اند ۸۵۲ سبز، پنل ۲۰۲ سبز،
اپ ۳۹ سبز (۱۹ استخراج، ۱۵ ماشین حالت، ۵ درز).
