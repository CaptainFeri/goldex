# برنامه فعال‌سازی تأمین‌کننده از پنل مدیریت (به‌همراه پروکسی per-provider)

سند برنامه‌ریزی بر پایه بررسی `goldex-sms-forwarder`، `goldex-pricing-engine/src/real-time-provider`،
`goldex-backend/src/provider` و `goldex-admin-panel/src/pages/ProvidersPage.tsx`.

> **وضعیت:** هر پنج فاز انجام شده است.
> بخش‌های «وضعیت فعلی» و «عیب‌های تاییدشده» وضعیت **پیش از** این کار را توصیف می‌کنند و
> عمداً دست‌نخورده مانده‌اند تا دلیل هر تغییر قابل ردیابی بماند.

خواسته: فعال‌سازی تأمین‌کننده — که امروز با اپ اندروید انجام می‌شود — کاملاً به پنل مدیریت منتقل شود و
**درست کار کند**؛ و چون این ترافیک باید از پشت پروکسی برود، این موضوع **در زمان تعریف تأمین‌کننده** اعلام شود.

---

## ۱. تصمیم‌های قفل‌شده

| # | موضوع | تصمیم |
|---|-------|-------|
| ۱ | مدل پروکسی | **فلگ بولی `useProxy` روی هر تأمین‌کننده** + همان پروکسی سراسری `PROXY_*` در env. رمز پروکسی وارد دیتابیس نمی‌شود. |
| ۲ | دامنه اثر فلگ | **همه ترافیک آن تأمین‌کننده**: OTP، REST (سفارش/موجودی/متادیتا) و WebSocket/SignalR |
| ۳ | اپ اندروید | **کاملاً حذف می‌شود**؛ تنها مسیر فعال‌سازی، پنل مدیریت است |
| ۴ | منبع کد OTP | ورود دستی توسط ادمینِ دارندهٔ سیم‌کارت (نتیجهٔ مستقیم تصمیم ۳) |
| ۵ | مرجع داده | `goldex-pricing-engine` همچنان مالک جدول `providers` است؛ جدول `provider` در بک‌اند آینهٔ ادمین می‌ماند |

---

## ۲. وضعیت فعلی

### ۲.۱ دو مسیر فعال‌سازی که امروز وجود دارد

**مسیر اندروید (کارکرد فعلی):** اپ، پنل وبِ خودِ تأمین‌کننده را در یک `WebView` باز می‌کند،
`assets/auth_interceptor.js` پاسخ لاگین را شنود می‌کند، `OtpNotificationListener` کد را از نوتیفیکیشن
برمی‌دارد و در فرم می‌گذارد، و در پایان `auth` + `active:true` را `PATCH` می‌کند.
گوشی داخل ایران است، پس اصلاً نیازی به پروکسی ندارد.

> نکته مهم: اپ به `GET /providers` و `PATCH /providers/{id}` می‌زند
> (`data/network/PricingEngineApi.kt`)، ولی **چنین روتی امروز در هیچ سرویسی وجود ندارد** —
> موتور قیمت‌گذاری headless شده (`goldex-pricing-engine/src/main.ts`) و روت بک‌اند
> `admin/providers` با گارد ادمین است. یعنی اپ در معماری فعلی عملاً از کار افتاده است.

**مسیر پنل (خراب):** پنل → `POST /admin/providers/:id/send-otp` و `verify-otp` → بک‌اند فقط یک فرمان
RabbitMQ منتشر می‌کند → موتور با `sendOtpUrl`/`verifyCodeUrl` به تأمین‌کننده درخواست می‌زند.
چون موتور خارج از ایران است، این مسیر **به پروکسی نیاز دارد**.

### ۲.۲ پروکسی امروز

`goldex-pricing-engine/src/common/http/proxy.config.ts` یک تصمیم **سراسری** است: اگر `PROXY_HOST`
ست باشد، همه‌چیز از پروکسی می‌رود مگر میزبان‌های بایپس (loopback، رنج‌های خصوصی، نام‌های تک‌بخشی داکر مثل `mock`).
هیچ کنترلی در سطح تأمین‌کننده وجود ندارد.

---

## ۳. عیب‌های تاییدشده (چرا «درست کار نمی‌کند»)

| # | عیب | محل | اثر |
|---|-----|-----|-----|
| ع‑۱ | فرم پنل هیچ ورودی‌ای برای `sendOtpUrl` / `verifyCodeUrl` / `webPanelUrl` **رندر نمی‌کند** (فقط در state هست) | `goldex-admin-panel/src/pages/ProvidersPage.tsx:55,76,102` | تأمین‌کنندهٔ ساخته‌شده از پنل هرگز آدرس OTP ندارد → فعال‌سازی از ریشه ناممکن |
| ع‑۲ | فالبک `provider.sendOtpUrl \|\| (() => \`${provider.sendOtpUrl}\`)()` به‌جای خطا، رشتهٔ `"undefined"` می‌سازد | `providers/zaryar-otp.handler.ts:10`، `talaab-otp.handler.ts:11` | درخواست به URL بی‌معنی می‌رود؛ خطا گمراه‌کننده است |
| ع‑۳ | `sendOtp`/`verifyOtp` بک‌اند فقط `publishCommand` می‌کنند و بی‌درنگ پیام موفقیت برمی‌گردانند | `goldex-backend/src/provider/provider.service.ts:216-241` | پنل همیشه «موفق» نشان می‌دهد، حتی وقتی کد اشتباه است |
| ع‑۴ | consumer موتور خطا را فقط `logger.error` می‌کند و رویداد شکست منتشر نمی‌شود | `provider-manage.consumer.ts:104-125` | خطا در لاگ کانتینر دفن می‌شود |
| ع‑۵ | موتور به `PROVIDER_OTP_SENT` / `PROVIDER_OTP_VERIFIED` منتشر می‌کند اما بک‌اند به آن‌ها subscribe نکرده | `rabbitmq/consumers/provider-status.consumer.ts:24-30` | آینهٔ ادمین بعد از فعال‌سازی موفق هم به‌روز نمی‌شود (تا تیک ۳۰ ثانیه‌ای رجیستری) |
| ع‑۶ | **`toPayload` بک‌اند `id` خودش را می‌فرستد** و موتور با `Object.assign(provider, data)` کلید اصلی را بازنویسی می‌کند | `goldex-backend/.../provider.service.ts:319` + `goldex-pricing-engine/.../provider.service.ts:102` | «ویرایش تأمین‌کننده» در موتور یا ردیف جدید می‌سازد یا روی unique-key می‌شکند — صامت، چون consumer فقط log می‌کند |
| ع‑۷ | در `findAll`، ردیف‌های آمده از رجیستری Redis که در آینه نیستند `id: undefined` می‌گیرند | `goldex-backend/.../provider.service.ts:114` | دکمه‌های پنل `/admin/providers/undefined/send-otp` صدا می‌زنند → ۴۰۰ از `ParseUUIDPipe` |
| ع‑۸ | بک‌اند پیش از ارسال OTP `active` را چک نمی‌کند، ولی موتور در این حالت `BadRequest` می‌اندازد | همان‌جا | no-op صامت |
| ع‑۹ | `priceUnit` در DTO پاسخ Swagger نیست (در بدنهٔ واقعی هست) | `provider/dto/provider-response.dto.ts` | فقط نقص سند |

ع‑۱ و ع‑۶ به‌تنهایی توضیح می‌دهند چرا فعال‌سازی از پنل هرگز به نتیجه نرسیده است.

---

## ۴. طراحی: فلگ `useProxy`

### ۴.۱ مدل داده

ستون `use_proxy boolean NOT NULL DEFAULT true` روی هر دو جدول:

| سرویس | جدول | مهاجرت |
|-------|------|--------|
| موتور | `providers` | `src/migrations/1700000000016-AddProviderUseProxy.ts` |
| بک‌اند | `provider` | `src/migrations/1000000000112-providerUseProxyMig.ts` |

**چرا پیش‌فرض `true`؟** تأمین‌کنندگان فعلی (talaab، zaryar) ایرانی‌اند و همین حالا هم از پروکسی می‌روند؛
ماک‌ها نام تک‌بخشی داکری دارند و قاعدهٔ بایپسِ موجود آن‌ها را بیرون نگه می‌دارد. پس پیش‌فرض `true`
رفتار امروز را **بی‌تغییر** بازتولید می‌کند.

### ۴.۲ مسیردهی — رجیستری میزبان

فایل جدید `goldex-pricing-engine/src/common/http/proxy-route.registry.ts`:
یک `Map<host, boolean>` در سطح ماژول، به‌علاوهٔ `register(host, useProxy)` و `lookup(host)`.

`RoutingHttpAgent.addRequest` / `RoutingHttpsAgent.addRequest` / `buildWebSocketAgent` به این ترتیب تصمیم می‌گیرند:

1. `isProxyBypassHost(host)` → **مستقیم** (بدون تغییر؛ mock و شبکهٔ داخلی همیشه مقدم‌اند)
2. میزبان در رجیستری هست → طبق فلگ همان تأمین‌کننده
3. در غیر این صورت → رفتار امروز (پروکسی)

**چرا این شکل و نه پاس‌دادن agent در هر فراخوانی؟** `HttpService` مشترک در ۱۰+ نقطه تزریق شده
(`provider-account.service.ts`، `provider-order.service.ts`، هندلرهای OTP) و مسیر WebSocket اصلاً از
axios رد نمی‌شود. رجیستری هر دو را با یک نقطهٔ تصمیم پوشش می‌دهد و صفر تغییر در محل فراخوانی‌ها می‌خواهد.

**محدودیتی که باید مستند شود:** تصمیم بر پایهٔ **میزبان** است، نه شناسهٔ تأمین‌کننده. اگر دو تأمین‌کننده
یک دامنه را به اشتراک بگذارند و فلگ‌شان مخالف باشد، «پروکسی برنده است» و یک `warn` لاگ می‌شود.

### ۴.۳ پرکردن رجیستری

`ProviderService` موتور در این نقاط رجیستری را sync می‌کند: `onApplicationBootstrap`، `create`، `update`،
`toggleActive`. میزبان‌ها از `baseUrl`، `apiBaseUrl`، `sendOtpUrl`، `verifyCodeUrl` و `config.originUrl`
استخراج می‌شوند. تابع `hostFromUrl` که امروز private است، export می‌شود.

### ۴.۴ انتشار فلگ

`useProxy` به این مسیرها اضافه می‌شود: `CreateProviderDto` هر دو سرویس، `toPayload` بک‌اند،
`upsertFromEngine`، `publishRegistry` (چون موجودیت کامل سریالایز می‌شود، خودکار است) و `ProviderDto` پاسخ.

---

## ۵. طراحی: بازخورد واقعی فعال‌سازی

بدون این بخش، پنل «کار می‌کند» ولی حقیقت را نشان نمی‌دهد. توصیه: **درخواست/پاسخ روی RabbitMQ**.

- `RabbitMQService` بک‌اند: متد `requestCommand(pattern, data, timeoutMs = 20000)` با
  `replyTo` (صف موقت exclusive) و `correlationId`.
- `ProviderManageConsumer` موتور: برای `SEND_OTP` و `VERIFY_OTP`، اگر `replyTo` روی پیام بود،
  نتیجه یا خطا را همان‌جا برمی‌گرداند (نه فقط `logger.error`).
- `ProviderController` بک‌اند: خطای برگشتی را به `BadRequestException` با **پیام واقعی موتور** تبدیل می‌کند
  («کد تایید نادرست است»، «آدرس verifyCodeUrl تنظیم نشده»، …).
- تایم‌اوت → پیام صریح «موتور قیمت‌گذاری پاسخ نداد».

مستقل از این، موتور باید `PROVIDER_OTP_FAILED` هم منتشر کند و بک‌اند به
`PROVIDER_OTP_SENT` / `PROVIDER_OTP_VERIFIED` / `PROVIDER_OTP_FAILED` گوش دهد تا آینه فوراً به‌روز شود (رفع ع‑۵).

**فالبک اگر RPC پذیرفته نشود:** جدول `provider_activation_attempt` در بک‌اند + نگاشت رویدادها روی آن +
پولینگ پنل. کارکرد مشابه، ولی پرهزینه‌تر و کندتر.

---

## ۶. تغییرات پنل مدیریت

`goldex-admin-panel/src/pages/ProvidersPage.tsx`:

1. **فرم تعریف/ویرایش** — افزودن ورودی‌های گم‌شده: `webPanelUrl`، `sendOtpUrl`، `verifyCodeUrl`
   (رفع ع‑۱) و پرکردن مقدار فعلی هنگام ویرایش (امروز به `""` ری‌ست می‌شوند).
2. **تیک «از پروکسی عبور کند»** با توضیح فارسی: «اگر تأمین‌کننده فقط از داخل ایران در دسترس است،
   این گزینه را روشن بگذارید». همان‌جا در فرم تعریف — دقیقاً خواستهٔ صورت‌مسئله.
3. **ستون/بج پروکسی** در جدول فهرست.
4. **بازنویسی `OtpModal`** به یک ویزارد دو مرحله‌ای با وضعیت واقعی: ارسال کد → شمارندهٔ ارسال مجدد →
   ورود کد → نمایش خطای برگشتی از بک‌اند به‌جای بستن صامت مودال.
5. **دکمهٔ «ورود با شبیه‌ساز»** — مسیر جهانی فعال‌سازی برای تأمین‌کنندگانی که کپچا دارند یا
   API لاگین‌شان قابل فراخوانی مستقیم نیست. طراحی کامل در بند ۷.
6. **راه فرار «ورود دستی توکن»** — یک فیلد برای الصاق دستی JSON احراز هویت (`token`، `uId`،
   `sessionId`، `shopkeeperId`) که `PATCH /admin/providers/:id` را با `auth` + `active` می‌زند.
   ارزان است و تا آماده‌شدن شبیه‌ساز، تنها جایگزین اپ اندروید است.
7. **`interface Provider`** — افزودن `useProxy`، `webPanelUrl`، `sendOtpUrl`، `verifyCodeUrl`.

---

## ۷. شبیه‌ساز مرورگر در پنل (حل کپچا توسط ادمین)

خواسته: پنل یک مرورگر باز کند، ادمین لاگین را مثل انسان انجام دهد و اگر کپچا آمد خودش حلش کند.
این دقیقاً همان کاری است که `WebView` اپ اندروید می‌کرد. سه راه برای بازسازی آن هست:

### ۷.۱ گزینه‌های رد شده

**الف) `<iframe>` مستقیم به سایت تأمین‌کننده — شدنی نیست.**
دو مانع مستقل: سایت‌ها `X-Frame-Options` / `frame-ancestors` می‌فرستند و اصلاً فریم نمی‌شوند؛ و
حتی اگر می‌شدند، iframe از مبدأ دیگر **کاملاً کدر** است — نه می‌توان `auth_interceptor.js` را تزریق کرد،
نه پاسخ‌ها را خواند. کل هدف ما برداشتن توکن است، پس این گزینه از ریشه بی‌فایده است.

**ب) پروکسی معکوس + بازنویسی صفحه (MITM) — شدنی ولی شکننده.**
بک‌اند سایت تأمین‌کننده را زیر مبدأ خودش سرو کند، HTML/JS را بازنویسی و اسکریپت شنود را تزریق کند؛
آن‌وقت iframe هم‌مبدأ می‌شود و همه‌چیز کار می‌کند. اما این یعنی نوشتن یک وب‌پروکسی واقعی:
بازنویسی URLهای مطلق و نسبی، `fetch`/XHR، WebSocket، دامنهٔ کوکی‌ها، `Set-Cookie`، حذف CSP،
و مسیریابی سمت‌کلاینت SPA. هر بار که تأمین‌کننده سایتش را عوض کند می‌شکند. نگهداری‌اش گران است.

### ۷.۲ گزینهٔ پیشنهادی: مرورگر سمت سرور + استریم به پنل

سرویس جدید `goldex-provider-browser` با Playwright/Chromium. ادمین در پنل دکمهٔ «ورود با شبیه‌ساز»
را می‌زند؛ سرویس یک `BrowserContext` تازه باز می‌کند، صفحهٔ لاگین تأمین‌کننده را می‌آورد، و تصویر زنده
به پنل استریم می‌شود. کلیک و تایپ ادمین به صفحه برمی‌گردد. **کپچا را ادمین با چشم و ماوس خودش حل می‌کند**،
چون یک مرورگر واقعی است. توکن هم از **بیرون صفحه** با CDP (`Network.responseReceived`) برداشته می‌شود.

**چرا این بهتر از (ب) است:** هیچ تزریقی و هیچ بازنویسی‌ای لازم نیست. شنود در لایهٔ شبکهٔ مرورگر اتفاق
می‌افتد، نه داخل صفحه — پس تغییر طراحی سایت تأمین‌کننده آن را نمی‌شکند.

**زیرساختی که از قبل داریم و دوباره استفاده می‌شود:**

| نیاز | چیزی که هست |
|------|-------------|
| کانال استریم دوطرفه با احراز هویت ادمین | `admin-notification.gateway.ts` (socket.io + وریفای JWT ادمین) و `admin-socket.ts` در پنل |
| خروجی از پشت پروکسی | همان `PROXY_*`؛ Playwright پروکسی را **per-context** می‌پذیرد → نگاشت یک‌به‌یک روی فلگ `useProxy` بند ۴ |

**جریان:**

1. `POST /admin/providers/:id/browser-session` → ساخت نشست، برگرداندن `sessionId`
2. پنل به namespace `admin-provider-browser` وصل می‌شود و `sessionId` را join می‌کند
3. سرویس با `Page.startScreencast` فریم‌های JPEG می‌فرستد؛ پنل روی `<canvas>` می‌کشد
4. رویدادهای ماوس/کیبورد پنل → `Input.dispatchMouseEvent` / `dispatchKeyEvent`
5. ادمین لاگین و کپچا و کد OTP را انجام می‌دهد
6. سرویس پاسخ حاوی `token`/`uId`/`sessionId`/`shopkeeperId` را می‌گیرد (همان منطق
   `ProviderAuthController.parseZaryarAuth` / `parseTalaabAuth` که از اپ اندروید منتقل می‌شود)
7. `auth` + `active:true` روی تأمین‌کننده ذخیره و به موتور فرمان داده می‌شود؛ نشست بسته می‌شود

**چرا سرویس جدا و نه داخل بک‌اند:** ایمیج بک‌اند `node:22-alpine` است و Playwright روی Alpine
رسماً پشتیبانی نمی‌شود. سرویس جدا با ایمیج رسمی Playwright، مصرف حافظهٔ مرورگر را هم از بک‌اند جدا نگه می‌دارد.

### ۷.۳ الزامات امنیتی (غیرقابل‌مذاکره)

این قابلیت یک «مرورگر قابل کنترل از راه دور **داخل شبکهٔ سرور**» است. بدون مهار زیر، به ابزار SSRF
تبدیل می‌شود که می‌تواند به postgres، redis، rabbitmq و هر سرویس داخلی دیگر برسد:

1. **لیست‌سفید ناوبری بر پایهٔ دامنهٔ همان تأمین‌کننده** — هر `request` خارج از آن دامنه `abort` شود.
   این مهم‌ترین بند است.
2. فقط `SUPER_ADMIN`؛ نشست‌ها در `admin-audit` ثبت شوند (چه کسی، چه تأمین‌کننده‌ای، چه زمانی).
3. یک نشست فعال در هر لحظه برای هر تأمین‌کننده، با TTL (مثلاً ۱۰ دقیقه) و teardown اجباری.
4. `BrowserContext` بدون persistence؛ بعد از هر نشست کاملاً دور ریخته شود.
5. سرویس مرورگر **هیچ پورتی بیرون شبکهٔ داکر منتشر نکند**؛ تنها راه دسترسی، بک‌اند باشد.

### ۷.۴ اثر روی بقیهٔ برنامه

شبیه‌ساز مسیر REST (`sendOtpUrl`/`verifyCodeUrl`) را **حذف نمی‌کند**، جایگزین آن هم نیست:

- مسیر REST **راه سریع** می‌ماند: دو فیلد، بدون مرورگر، مناسب تأمین‌کننده‌ای که API لاگین ساده دارد.
- شبیه‌ساز **راه جهانی** است: برای کپچا، لاگین چندمرحله‌ای، یا تأمین‌کنندهٔ جدیدی که هنوز API لاگینش
  مهندسی معکوس نشده. با آن، `sendOtpUrl`/`verifyCodeUrl` از «اجباری» به «اختیاری» تنزل می‌کنند.

نکته‌ای که شبیه‌ساز حل نمی‌کند: **کد پیامکی همچنان روی سیم‌کارت است** و ادمین باید تایپش کند.
شبیه‌ساز محل تایپ را عوض می‌کند، نه منبع کد را.

---

## ۸. حذف اپ اندروید

| مورد | اقدام |
|------|-------|
| `goldex-sms-forwarder/` | حذف کامل دایرکتوری |
| `.github/workflows/build.yml` | حذف (تنها کارش ساخت APK همین اپ است) |
| `README.md` خطوط ۲۳، ۷۶، ۹۴، ۱۴۰ | حذف از نمودار معماری، جدول سرویس‌ها، پیش‌نیازها و درخت پوشه‌ها |
| سمت سرور | **چیزی برای پاک‌سازی نیست** — روت‌هایی که اپ صدا می‌زد (`GET /providers`، `PATCH /providers/{id}`) امروز اصلاً وجود ندارند |

---

## ۹. مهاجرت‌ها

```
goldex-pricing-engine/src/migrations/1700000000016-AddProviderUseProxy.ts
goldex-backend/src/migrations/1000000000112-providerUseProxyMig.ts
```

هر دو: `ADD COLUMN use_proxy boolean NOT NULL DEFAULT true`، با `down` متقارن.
هیچ backfill لازم نیست، چون پیش‌فرض دقیقاً رفتار امروز است.

---

## ۱۰. فازبندی

| فاز | محتوا | چرا این ترتیب |
|-----|-------|----------------|
| **۰** ✅ | رفع ع‑۱، ع‑۲، ع‑۶، ع‑۷ | مسدودکننده‌اند؛ تا اینجا درست نشود هیچ فعال‌سازی‌ای از پنل ممکن نیست |
| **۱** ✅ | فلگ `useProxy`: مهاجرت + رجیستری + انتشار + UI فرم | خواستهٔ اصلی صورت‌مسئله |
| **۲** ✅ | بازخورد واقعی: RPC + `PROVIDER_OTP_FAILED` + consumer بک‌اند (ع‑۳، ع‑۴، ع‑۵، ع‑۸) | «درست کار کند» بدون این محقق نمی‌شود |
| **۳** ✅ | بازسازی UI فعال‌سازی + ورود دستی توکن | ارزان‌ترین جایگزین اپ اندروید؛ پیش‌نیاز فاز ۴ |
| **۴** ✅ | حذف اپ اندروید و پاک‌سازی مستندات | آخر از همه، تا پیش از آماده‌بودن جایگزین، راه فعلی قطع نشود |
| **۵** ✅ | شبیه‌ساز مرورگر (بند ۷) | بزرگ‌ترین فاز و تنها فازی که سرویس جدید می‌آورد؛ جدا نگه داشته می‌شود تا فازهای ۰ تا ۴ را به تعویق نیندازد |

فاز ۵ عمداً بعد از ۴ است، نه به‌جای آن: «ورود دستی توکن» در فاز ۳ ظرف چند ساعت کار، همان نتیجهٔ نهایی
(ذخیرهٔ `auth` و فعال‌سازی) را می‌دهد و ریسک حذف اپ را می‌پوشاند؛ شبیه‌ساز همان کار را **راحت** می‌کند،
نه **ممکن**. اگر ترجیح شما شروع زودتر شبیه‌ساز است، فاز ۵ می‌تواند موازی با ۱ و ۲ پیش برود — تنها
وابستگی‌اش فلگ `useProxy` از فاز ۱ است.

---

## ۱۱. تست

| سطح | تست |
|-----|-----|
| موتور | `common/http/proxy.config.spec.ts` — جدول مسیردهی: بایپس مقدم، رجیستری `false` → مستقیم، رجیستری `true` → پروکسی، ناشناخته → پیش‌فرض |
| موتور | هندلرهای OTP بدون `sendOtpUrl`/`verifyCodeUrl` باید خطای صریح بدهند، نه URL `"undefined"` |
| موتور | `update()` نباید `id` را عوض کند، حتی وقتی payload شامل `id` است (ع‑۶) |
| موتور | `startProvider` باید میزبان‌های تأمین‌کننده را در رجیستری ثبت کند |
| بک‌اند | `toPayload` نباید `id` بفرستد؛ `findAll` نباید ردیف بدون `id` تولید کند |
| بک‌اند | `verifyOtp` با خطای موتور باید `BadRequest` با پیام واقعی بدهد، نه پیام موفقیت |
| پنل | بدنهٔ ارسالی فرم شامل `useProxy` و هر سه آدرس باشد؛ ویرایش مقادیر موجود را حفظ کند |
| شبیه‌ساز | ناوبری به دامنه‌ای خارج از لیست‌سفید باید `abort` شود (تست امنیتی اجباری بند ۷٫۳) |
| شبیه‌ساز | نشست پس از TTL و پس از قطع اتصال ادمین، حتماً teardown شود |
| شبیه‌ساز | پارسر پاسخ زریار/طلاب همان خروجی `parseZaryarAuth`/`parseTalaabAuth` اپ اندروید را بدهد |

---

## ۱۲. ریسک‌ها و نکات باز

1. **کپچا.** بدنهٔ verify زریار فیلد `CaptchaToken` دارد (`providers/zaryar-otp.handler.ts`).
   مسیر `WebView` اپ اندروید ذاتاً از کپچا عبور می‌کرد؛ مسیر REST سمت سرور نمی‌کند.
   دو پاسخ داریم: **ورود دستی توکن** (فاز ۳، ارزان) و **شبیه‌ساز مرورگر** (فاز ۵، بند ۷).
   تا وقتی هیچ‌کدام نیامده، حذف اپ اندروید نباید انجام شود.
2. **کد OTP روی سیم‌کارتی است که دست ادمین پنل نیست.** پس از حذف اپ، این یک فرایند انسانی می‌شود
   (تماس با دارندهٔ سیم‌کارت). اگر بعداً آزاردهنده شد، فورواردر SMS بدون هیچ UI —
   فقط یک وب‌هوک به بک‌اند — می‌تواند برگردد.
3. **اشتراک دامنه بین تأمین‌کنندگان** — بند ۴٫۲؛ امروز مصداق ندارد ولی باید لاگ هشدار داشته باشد.
4. **نبود پروکسی در env** (`PROXY_HOST` خالی) → فلگ بی‌اثر است و همه‌چیز مستقیم می‌رود.
   پنل باید این وضعیت را نشان دهد تا ادمین تیکِ بی‌اثر نزند؛ یک فیلد `proxyConfigured` در پاسخ وضعیت کافی است.
   **در فاز ۱ پیاده نشد** — نیازمند رساندن یک واقعیت از موتور به بک‌اند است، نه یک تغییر تک‌خطی. باز است.
5. **`goldex-cbp` پروکسی مستقل خودش را دارد** (`CBP_PROXY_*`، `SHAHIN_PROXY_*`). خارج از دامنهٔ این کار است و دست‌نخورده می‌ماند.
6. **شبیه‌ساز = سطح حملهٔ جدید.** مرورگر قابل کنترل از راه دور داخل شبکهٔ سرور، اگر لیست‌سفید ناوبری
   نداشته باشد یک ابزار SSRF تمام‌عیار است. بند ۷٫۳ پیش‌نیاز ادغام است، نه بهبود بعدی.
7. **هزینهٔ اجرایی شبیه‌ساز** — یک کانتینر Chromium (ایمیج ~۱ گیگابایت، چند صد مگابایت RAM به‌ازای هر نشست فعال).
   نشست‌ها کوتاه و تک‌به‌تک‌اند، پس بار پایدار نیست؛ ولی در ظرفیت‌سنجی سرور باید دیده شود.
