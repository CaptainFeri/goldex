# برنامه بازطراحی انبار (Warehouse V2)

سند برنامه‌ریزی بر پایه بررسی `goldex-backend/src/warehouse` و ماژول‌های مرتبط
(`admin-accounting`، `provider-finance`، `admin-inbox`)، و خواسته‌های تکمیلی کارفرما.

مرجع دامنه: `warehouse-roadmap.html` — مخرج مشترک همه محاسبات، گرم طلای عیار ۷۵۰ است و
`وزن باطنی (۷۵۰) = (وزن ظاهری × عیار) / ۷۵۰`.

---

## ۱. تصمیم‌های قفل‌شده

| # | موضوع | تصمیم |
|---|-------|-------|
| ۱ | معنای «سند واریز/برداشت» | سند حسابداری در `accounting_vouchers` |
| ۲ | وضعیت سند خودکار | مستقیماً **FINALIZED** (بدون چرخه draft→pending) |
| ۳ | مالکیت بسته پس از ورود | بسته **سیستمی** است؛ اولویت «سپرده قبلی کاربر» در الگوریتم تخصیص حذف می‌شود |
| ۴ | ردیابی انسانی | فرستنده و مدیر تحویل‌گیرنده در ورود، و گیرنده و مدیر تحویل‌دهنده در خروج، ثبت می‌شوند |
| ۵ | دامنه برداشت | هر بسته‌ای که وارد انبار شد، برای **همه کاربران** قابل برداشت است |
| ۶ | مغایرت وزن | وزن اعلامی کاربر فقط یک «اعلام» است؛ مبنای اعتبار، **وزن باطنی واقعی** بسته‌های تحویلی است |
| ۷ | تسویه سرویس‌دهنده | طلای دریافتی از سرویس‌دهنده تا زمان بسته‌بندی، «طلای بسته‌بندی‌نشده» است و باید به مدیر انبار اطلاع داده شود |
| ۸ | مبنای شارژ کیف پول | **عدد تاییدشدهٔ ادمین**، نه وزن اعلامی کاربر. بدون سقف و بدون تاییدیهٔ اضافی برای مغایرت |
| ۹ | تعویض‌پذیری بسته | بسته‌ها در انبار کاملاً fungible هستند: بستهٔ واردشده توسط کاربر۱ می‌تواند به درخواست برداشت کاربر۲ تخصیص یابد |
| ۱۰ | بسته‌های موجود | بسته‌های `IN_WAREHOUSE` که اکنون به کاربران چسبیده‌اند، در مهاجرت به استخر سیستم منتقل می‌شوند و مالک فعلی‌شان به `sender_user_id` می‌رود |

---

## ۲. وضعیت فعلی

### ۲.۱ نقشه ماژول

| فایل | نقش |
|------|-----|
| `service/warehouse.service.ts` | CRUD انبار، ظرفیت، آمار روز، تراز مواد تسویه |
| `service/packet.service.ts` | ساخت بسته، QC، شکستن بسته با بقای جرم، بسته تسویه |
| `service/warehouse-request.service.ts` | چرخه کامل درخواست ورود/خروج (۱۶۷۳ خط) |
| `service/allocation.service.ts` | الگوریتم تخصیص هوشمند بند ۳ روادمپ |
| `entity/packet.entity.ts` | بسته فیزیکی |
| `entity/warehouse-request.entity.ts` | درخواست ورود/خروج |
| `entity/warehouse-history.entity.ts` | Audit trail |

### ۲.۲ جریان فعلی واریز

```
POST /warehouse/deposit                    (کاربر، وزن اعلامی)
  → درخواست PENDING
  → PUT /admin/warehouse/requests/:id/process  (APPROVED)
      → processInputApproval()  ← یک بسته PENDING به مالکیت کاربر می‌سازد
  → PUT /admin/warehouse/requests/:id/confirm-material
      → وزن باطنی بازمحاسبه، بسته IN_WAREHOUSE، شارژ کیف پول
```

### ۲.۳ جریان فعلی برداشت

```
POST /warehouse/withdraw  → قفل موجودی آزاد → PENDING
  → تخصیص (AllocationService) → APPROVED
  → COMPLETED → تحویل فیزیکی، بازگشت مابه‌التفاوت دیجیتال، آزادسازی قفل
```

---

## ۳. شکاف‌ها

### ش-۱ — سند حسابداری صادر نمی‌شود

هیچ مسیری در انبار `accounting_vouchers` نمی‌نویسد. دسته‌های `DEPOSIT_ENTRY` و
`WITHDRAW_ENTRY` وجود دارند ولی فقط دستیِ حسابدار پر می‌شوند.

ضمناً `syncLinkedRecord` (`warehouse-request.service.ts:1321`) روی
`deposit.warehouse_request_id` و `withdraw.warehouse_request_id` کار می‌کند، اما
**هیچ کدی این ستون‌ها را پر نمی‌کند** — کد مرده است و باید حذف یا تعیین‌تکلیف شود.

### ش-۲ — مبنای اعتبار بین دو مسیر واریز ناسازگار است  ⚠️ باگ

دو مسیر مختلف واریز را تکمیل می‌کنند و **دو عدد متفاوت** به کیف پول می‌ریزند:

| مسیر | ظرفیت انبار | اعتبار کیف پول |
|------|-------------|----------------|
| `confirmDepositMaterial` (خط ۳۳۶، ۳۴۳) | `packet.pureWeight` ✅ واقعی | `packet.pureWeight ?? request.weight` ✅ واقعی |
| `processInputCompletion` (خط ۵۵۴، ۵۵۷) | `request.weight` ❌ اعلامی | `request.weight` ❌ اعلامی |

یعنی سناریوی «اعلام ۱۰۰ گرم، واقعی ۹۶ گرم» بسته به اینکه ادمین کدام دکمه را بزند،
۹۶ یا ۱۰۰ گرم شارژ می‌کند. این مستقیماً همان موردی است که کارفرما به آن اشاره کرد.

### ش-۳ — تقسیم بسته در زمان ورود ممکن نیست

`splitWithParts` فقط روی بسته `IN_WAREHOUSE`/`ORPHAN` کار می‌کند، ولی بسته در لحظه
تحویل `PENDING` است. `ConfirmMaterialDto` هم تک‌بسته است. پس ادمین مجبور است اول یک
بسته یکپارچه ثبت کند و بعد در عملیات جداگانه آن را بشکند.

### ش-۴ — بسته واریزی سیستمی نمی‌شود و فرستنده/تحویل‌گیرنده ثبت نمی‌شود

`processInputApproval` بسته را با `userId: request.userId` و `isOrphan: false`
می‌سازد (خطوط ۵۱۰ و ۵۱۵) — یعنی مالکیت فیزیکی به کاربر می‌چسبد.
`PacketEntity` هیچ فیلدی برای فرستنده، مدیر تحویل‌گیرنده، گیرنده یا مدیر
تحویل‌دهنده ندارد؛ فقط `metadata` آزاد.

### ش-۵ — تخصیص، انبارمحور و جامع نیست

- `suggestForRequest` بسته‌های کاربر را در اولویت می‌گذارد (خلاف تصمیم ۳).
- فقط «بهترین تکِ کمتر» و یک ترکیب برمی‌گرداند، نه **لیست همه موارد ≤ درخواست**.
- روی `symbolId` فیلتر نمی‌کند — درخواست طلا می‌تواند بسته نقره بگیرد. ⚠️ باگ
- `PacketEntity` اصلاً `symbolId` ندارد.

### ش-۶ — پنل کاربر انبار را نمی‌فرستد  ⚠️ باگ

`goldex-user-panel/src/pages/WarehousePage.jsx:124` فقط `weight`, `symbolId`, `notes`
می‌فرستد، درحالی‌که `CreateWithdrawRequestDto.warehouseId` اجباری است
(`@IsUUID()` بدون `@IsOptional()`). **هر برداشت از پنل کاربر همین الان ۴۰۰ می‌گیرد.**

### ش-۷ — طلای بسته‌بندی‌نشده تسویه، رصد نمی‌شود

`getSettlementMaterialBalance` (`warehouse.service.ts:263`) فقط
`Σ RECEIVE − Σ PAY` را از جدول `provider_settlements` می‌خواند.
`createFromSettlement` بسته می‌سازد ولی:

- هیچ لینکی به ردیف تسویه یا `providerKey` ساختاریافته ندارد (فقط رشته `batchNumber`)،
- تراز را کم نمی‌کند،

پس عددی که به ادمین نشان داده می‌شود **هرگز با بسته‌بندی کاهش نمی‌یابد** و «مانده
بسته‌بندی‌نشده» اساساً وجود ندارد. هیچ اطلاع‌رسانی‌ای هم به مدیر انبار نمی‌رود.

### ش-۸ — باگ‌های جانبی

| مورد | محل | شرح |
|------|-----|-----|
| کد سند در برابر همزمانی امن نیست | `admin-accounting.service.ts:410` | `COUNT + LIKE` → دو سند همزمان یک کد می‌گیرند و به خطای unique می‌خورند |
| انگی از ظرفیت کم نمی‌شود | `packet.service.ts` `splitWithParts` | مادر بایگانی و فرزندان ساخته می‌شوند بدون `updateCapacity`؛ گرم‌های انگی در ظرفیت باقی می‌مانند |
| کسر دوباره در رد واریز | `warehouse-request.service.ts:790` | `unlockWalletForRejectedDeposit` هم از `locked` هم از `free` کم می‌کند |
| هیچ تستی وجود ندارد | — | هیچ `*.spec.ts` برای انبار/بسته/تخصیص نیست |

---

## ۴. برنامه اجرا

### فاز ۰ — پایه داده

**مهاجرت `packet`:**

| ستون | نوع | شرح |
|------|-----|-----|
| `symbol_id` | uuid | نماد مادهٔ داخل بسته (رفع ش-۵) |
| `sender_user_id` | uuid | چه کسی آن را آورده |
| `received_by_admin_id` | uuid | کدام مدیر تحویل گرفته |
| `delivered_to_user_id` | uuid | در خروج، به چه کسی تحویل شد |
| `delivered_by_admin_id` | uuid | در خروج، کدام مدیر تحویل داد |
| `source_request_id` | uuid | درخواست انبارِ مبدأ |
| `provider_key` | varchar(100) | برای بسته‌های تسویه (رفع ش-۷) |
| `settlement_id` | uuid | ردیف تسویه مبدأ |

ایندکس تخصیص: `(warehouse_id, status, symbol_id, pure_weight)`.

**مهاجرت `warehouse_request`:** `voucher_id uuid`, `declared_weight decimal(20,8)`,
`actual_weight decimal(20,8)`.

**بک‌فیل:** `sender_user_id` از `packet.user_id` فعلی؛ `provider_key` با پارس
`batch_number` الگوی `STL-<provider>-<ts>`.

---

### فاز ۱ — سند حسابداری خودکار (FINALIZED)

1. **اول** `nextVoucherCode` را با یک sequence دیتابیسی امن کنیم (ش-۸). بدون این،
   سه منبع خودکارِ جدید سند، خطای unique تولید می‌کنند.

2. متد جدید در `AdminAccountingService`:

   ```ts
   issueSystemVoucher(input: SystemVoucherInput): Promise<AccountingVoucherEntity>
   ```

   سند را مستقیماً `FINALIZED` می‌سازد. **این عمداً کنترل دوگانه
   (`assertReviewable` / `SELF_REVIEW_FORBIDDEN`) را دور می‌زند** — تصمیم ۲ کارفرما.
   `createdBy` = ادمین عامل، `reviewedBy` = همان ادمین، `reviewedAt` = اکنون،
   و `extraDescription` منبع سیستمی را مشخص می‌کند تا در گزارش‌ها از سند دستی
   قابل تفکیک باشد.

3. سه نقطهٔ صدور، **درون همان تراکنش** عملیات:

   | رویداد | movement | category |
   |--------|----------|----------|
   | تکمیل واریز انبار | `deposit` | `DEPOSIT_ENTRY` |
   | تکمیل برداشت انبار | `withdraw` | `WITHDRAW_ENTRY` |
   | تسویه با سرویس‌دهنده | از `direction` | `CUSTOMER_SETTLEMENT` |

   مبلغ سند **همیشه وزن باطنی واقعی** است، نه اعلامی.

4. `warehouse` باید `AdminAccountingModule` را import کند — چرخه ندارد چون
   accounting به انبار وابسته نیست.

5. `syncLinkedRecord` و کد مردهٔ `warehouse_request_id` حذف یا مستند شود.

---

### فاز ۲ — ورود با تقسیم و وزن واقعی

**رفع ش-۲ و ش-۳.**

- `ConfirmMaterialDto` به `parts[] + wastage` گسترش می‌یابد؛ ادمین در همان تراکنشِ
  تحویل، n بسته ثبت می‌کند.
- برای هر جزء: `وزن باطنی = (ظاهری × عیار) / ۷۵۰`.
- بقای جرم: `Σ وزن باطنی اجزا + انگی` باید با وزن باطنی کلِ توزین‌شده بخواند.
- **اعتبار کیف پول = `Σ وزن باطنی اجزا`** (سناریوی ۱۰۰ اعلامی / ۹۶ واقعی → ۹۶ شارژ).
- `processInputCompletion` یا حذف می‌شود یا به همان منطق `confirmDepositMaterial`
  منتقل می‌شود تا دو مبنای متفاوت باقی نماند.
- مغایرت اعلامی/واقعی در `warehouse_request.declared_weight` و `actual_weight`
  ذخیره و در `warehouse_history` با اکشن `DEPOSIT_WEIGHT_VARIANCE` ثبت می‌شود.
- مغایرت هر اندازه که باشد پذیرفته می‌شود؛ عدد تاییدشدهٔ ادمین قطعی است (تصمیم ۸).
  سقف و تاییدیهٔ دوم در کار نیست — مغایرت فقط ثبت و قابل حسابرسی می‌شود.

---

### فاز ۳ — بسته سیستمی + ردیابی انسانی

**رفع ش-۴.**

- بسته‌های ورودی: `user_id = null`, `is_orphan = true`, `status = ORPHAN`،
  با `sender_user_id` و `received_by_admin_id` پرشده.
- در خروج: `delivered_to_user_id` و `delivered_by_admin_id` پر می‌شوند.
- شاخه‌های `own-exact` / `own-fit` و split بسته کاربر در `AllocationService` حذف
  می‌شوند؛ `approveWithdrawForOutput` هم متناسباً ساده می‌شود.
- `GET /warehouse/packets` کاربر معنایش عوض می‌شود: «بسته‌هایی که من آورده‌ام یا
  تحویل گرفته‌ام» به‌جای «بسته‌های من».
- پنل ادمین: ستون فرستنده/تحویل‌گیرنده در جدول بسته‌ها.

---

### فاز ۴ — تخصیص انبارمحور

**رفع ش-۵ و ش-۶.**

- endpoint کاربری:
  `GET /warehouse/allocation-options?warehouseId&symbolId&weight`
  که **همه** بسته‌های سیستمیِ آن انبار با `pure_weight <= weight` را همراه
  مابه‌التفاوت بازگشتی برمی‌گرداند (نه فقط بهترین).
- فیلتر `symbol_id` در کل مسیر تخصیص.
- وضعیت جدید `RESERVED` در `PacketStatusEnum` + قفل بدبینانه، تا دو کاربر همزمان یک
  بسته را نگیرند.
- تلورانس ۰٫۰۵ گرم طبق بند ۴ روادمپ حفظ می‌شود.
- **پنل کاربر:** افزودن انتخابگر انبار به فرم برداشت و نمایش لیست بسته‌های قابل
  تخصیص — این تنها راه رفع ش-۶ است.

---

### فاز ۵ — تسویه سرویس‌دهنده و طلای بسته‌بندی‌نشده

**رفع ش-۷.** خواستهٔ جدید کارفرما.

```
POST /admin/provider-finance/settle   (direction=RECEIVE, symbol=XAU)
  ├─ ثبت ردیف provider_settlements            (موجود)
  ├─ صدور سند حسابداری FINALIZED               (جدید — فاز ۱)
  ├─ افزودن به مانده «طلای بسته‌بندی‌نشده»      (جدید)
  └─ رویداد → کارتابل مدیر انبار                (جدید)

POST /admin/warehouse/settlement-material/release
  ├─ ساخت بسته با provider_key و settlement_id  (اصلاح)
  └─ کاهش مانده بسته‌بندی‌نشده                   (جدید)
```

- بازنویسی `getSettlementMaterialBalance` به فرمول:
  `بسته‌بندی‌نشده = Σ RECEIVE − Σ PAY − Σ (وزن باطنی + انگیِ بسته‌های ساخته‌شده از تسویهٔ آن سرویس‌دهنده)`
  اتکا به `provider_key` ستونی، نه پارس رشتهٔ `batch_number`.
- `createFromSettlement` باید `provider_key` و `settlement_id` را پر کند و بیش از
  ماندهٔ بسته‌بندی‌نشده اجازهٔ ترخیص ندهد.
- **اطلاع‌رسانی** از مکانیزم موجود `AdminInboxService.publish` استفاده می‌کند
  (پایدار + websocket، با `requiredPermission` که کارتابل را به نقش مربوطه محدود
  می‌کند). کلید مجوز `warehouse` از قبل در `admin-role/permission.catalog.ts:33`
  وجود دارد:

  ```ts
  { event: "warehouse.unpackaged_material",
    category: InboxCategory.SYSTEM,
    severity: InboxSeverity.WARNING,
    title: "طلای بسته‌بندی‌نشده در انتظار اقدام",
    requiredPermission: "warehouse" }
  ```

- `InboxCategory` احتمالاً به مقدار `WAREHOUSE` نیاز دارد.
- کرون یادآور برای ماندهٔ بسته‌بندی‌نشده‌ای که بیش از N ساعت باز مانده.
- پنل ادمین: کارت «بسته‌بندی‌نشده» در `WarehousePage.tsx` (فرم ترخیص از قبل هست،
  خط ۱۲۷۱ و ۱۴۴۵) و نشانگر در `ProviderFinancePage.tsx`.

---

### فاز ۶ — تست و پایداری

- تست دیتابیسی: بقای جرم در ورودِ چندبسته‌ای؛ مغایرت اعلامی/واقعی؛ تخصیص و تلورانس؛
  صدور سند؛ تراز بسته‌بندی‌نشده.
- تست همزمانی: دو برداشت همزمان روی یک بسته.
- رفع باگ‌های ش-۸ (انگی در ظرفیت، کسر دوباره در رد واریز).

---

## ۵. ترتیب و وابستگی

```
فاز ۰ (پایه داده)
  ├─→ فاز ۱ (سند)  ──→ فاز ۵ (تسویه)
  ├─→ فاز ۲ (ورود چندبسته‌ای + وزن واقعی)
  ├─→ فاز ۳ (بسته سیستمی)  ──→ فاز ۴ (تخصیص + پنل کاربر)
  └─→ فاز ۶ (تست)
```

فاز ۱ به `nextVoucherCode` امن وابسته است و باید قبل از هر صدور خودکار انجام شود.
فاز ۴ به فاز ۳ وابسته است چون تا بسته‌ها سیستمی نشوند، تخصیص انبارمحور معنا ندارد.

## ۶. اصل تعویض‌پذیری

بسته پس از ورود به انبار، یک واحد قابل معاملهٔ بی‌نام است. «چه کسی آن را آورد» و
«چه کسی آن را برد» دو **واقعیت تاریخی** ثبت‌شده روی بسته‌اند، نه ادعای مالکیت:

- `sender_user_id` / `received_by_admin_id` هنگام ورود نوشته و دیگر تغییر نمی‌کنند.
- `delivered_to_user_id` / `delivered_by_admin_id` هنگام خروج نوشته می‌شوند.
- `user_id` در تمام مدتی که بسته در انبار است `NULL` می‌ماند.

پس بستهٔ واردشده توسط کاربر۱ کاملاً مجاز است به درخواست برداشت کاربر۲ تخصیص یابد،
و زنجیرهٔ حسابرسی هر دو طرف را برای همیشه نگه می‌دارد. موجودی کاربر۱ در کیف پول
دیجیتالش زندگی می‌کند، نه در فلز مشخصی داخل انبار.
